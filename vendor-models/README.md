These files used to ship inside the IPA. They are **not** copied into the app target.

After install the app downloads ASR/LLM weights from Hugging Face
(`ggerganov/whisper.cpp`, `mlx-community/Qwen3-0.6B-4bit`).
